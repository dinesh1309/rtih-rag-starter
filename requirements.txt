# Pinned to versions verified working on 2026-06-10 — everyone gets the same set.
# Install:  pip install -r requirements.txt   (large download, ~2 min — that's normal, langchain is big)

langchain==0.3.30
langchain-community==0.3.31
langchain-core==0.3.86
langchain-text-splitters==0.3.11
langchain-chroma==0.2.6
langchain-openai==0.3.35
langchain-anthropic==0.3.22
fastembed==0.7.4
python-dotenv==1.2.1

# Optional — uncomment ONLY the loader you need (see the LOAD section in rag_starter.py):
# pypdf            # PDFs
# docx2txt         # Word .docx
# unstructured     # emails (.eml) and other rich formats
# beautifulsoup4   # HTML / web pages
# pandas           # logs, profiles, CSV/JSON tables (DataFrameLoader)
